apply plugin: 'java'

defaultTasks 'jar'

dependencies {
    compile files('lib/*')
}

jar {
    manifest {
        attributes 'Main-Class' : '' // ??? 
        attributes 'Class-Path' : '../../lib/*'

    }
}

